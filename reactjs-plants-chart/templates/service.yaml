apiVersion: v1
kind: Service
metadata:
  name: {{ include "reactjs-plants-chart.fullname" . }}
  labels:
    {{- include "reactjs-plants-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reactjs-plants-chart.selectorLabels" . | nindent 4 }}
