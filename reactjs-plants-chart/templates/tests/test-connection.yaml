apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reactjs-plants-chart.fullname" . }}-test-connection"
  labels:
    {{- include "reactjs-plants-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reactjs-plants-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
